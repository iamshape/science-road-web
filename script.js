const menuButton=document.querySelector('.mobile-toggle');const menu=document.querySelector('.nav');menuButton?.addEventListener('click',()=>menu.classList.toggle('mobile-open'));const modal=document.querySelector('.modal');const opens=document.querySelectorAll('.open-contact');const close=document.querySelector('.close');let last;function openModal(e){last=e.currentTarget;modal.classList.add('open');modal.setAttribute('aria-hidden','false');close.focus()}function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');last?.focus()}opens.forEach(x=>x.addEventListener('click',openModal));close?.addEventListener('click',closeModal);modal?.addEventListener('click',e=>{if(e.target===modal)closeModal()});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal?.classList.contains('open'))closeModal()});
